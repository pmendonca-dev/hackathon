[project]
name = "aval-rail-selector-prototype"
version = "0.1.0"
description = "Deterministic, non-integrated rail selection prototype"
requires-python = ">=3.12,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers --strict-config"
